## All authored by Claude Sonnet 4.6
### Prompts:
#### Generation
```
using the same grammar as is implemented in the ./procedures directory, write an LL(1) parser in 
./ll/ ; 
you should only modify / add / delete in ./ll/ and nowhere else, and 
use java.util.Scanner instead of the implemented scanner in ./scanner/
```

#### Checkstyle and Javadocs
```
do two tasks:
1. run "checkstyle -c checkstyle.xmll ll/*.java" and fix all the errors that the checkstyle reports.
2. complete full javadocs documentation in all of the relevant files in ./ll/
```
